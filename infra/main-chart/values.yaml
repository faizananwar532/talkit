namespace: prod

superuser_email: 

images:
  auth: gcr.io/talkit-prod/auth
  chat: gcr.io/talkit-prod/chat
  web: gcr.io/talkit-prod/web
  
tag:

pullpolicy: Always

hostnames:
  web: talkit.fun
  api: api.talkit.fun

buckets:
  content: profile-content

google:
  sa_key:
  project: talkit-prod
  oauthClientID:
  oauthClientSecret:
  oauthCallbackURL: https://api.talkit.fun/v1/oauth/google/callback
 
secrets:
  accessTokenKey:
  encryptionSaltRounds: 10
  newUserPassword: 1s28fnv9f
  refreshTokenKey:
  userActivationTokenKey:

mailgun:
  apiKey:
  domain: mg.talkit.fun

redis:
  server: redis-10121.c15924.us-central1-mz.gcp.cloud.rlrcp.com
  password: 
  userCreatedChannel: user_created
  userInitationChannel: user_invitation

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 70
  
postgresql:
  connectionName: talkit-prod:us-central1:talkit-guard
  port: 5432
  postgresqlUsername: postgres
  postgresqlPassword:

databases:
  auth: auth

ports:
  auth: 4000
  chat: 3060
  redis: 10121
  web: 80
